import { Typography } from "@mui/material";

function Header () {
    return (
        <Typography gutterBottom variant= 'h4' sx = {{color: 'text.secondary'}}>Log In</Typography>
    )
}

export default Header